# Warehouse file — not committed (each clone regenerates it)
warehouse/*.duckdb
warehouse/*.duckdb.wal

# Downloaded raw data files (large; gitignored)
data/
*.parquet

# Python virtual environment and cache
.venv/
venv/
__pycache__/
*.pyc

# dbt outputs
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml

# macOS
.DS_Store
